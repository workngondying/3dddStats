{
  "name": "sitestat-github-pages",
  "version": "1.0.0",
  "private": true,
  "description": "Static 3ddd stats site for GitHub Pages",
  "scripts": {
    "collect": "node scripts/collect.js",
    "build": "node scripts/build-static-data.js",
    "collect:build": "node scripts/collect.js && node scripts/build-static-data.js"
  }
}
